import { SpaceShowPipe } from './space-show.pipe';

describe('SpaceShowPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceShowPipe();
    expect(pipe).toBeTruthy();
  });
});
